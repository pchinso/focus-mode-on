# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
build/
dist/

# Environment / secrets
.env

# Personal vault — never tracked. The app creates work/ and personal/ on
# startup; keep your Markdown data local (or in a separate private repo).
vault/
