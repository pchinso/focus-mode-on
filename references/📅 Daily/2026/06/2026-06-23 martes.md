

# TASK
## INBOX



## DONE

**M2D — hecho hoy (23 jun 2026)**

**Fix crítico exe PyInstaller**

- El exe distribuía 0 toolboxes visibles (solo Bienvenida + Proyectos). Causa: `pkgutil.iter_modules` no enumera paquetes congelados en PyInstaller, y `collect_submodules` devolvía lista vacía porque la raíz no estaba en `sys.path` al evaluar el `.spec`.
- Fix en tres frentes: `registry.py` con lista de respaldo `_KNOWN_TOOLBOXES`, `m2d.spec` con `sys.path` + `hiddenimports` explícitos por toolbox, selftest reforzado que falla si encuentra < 6 toolboxes o páginas insuficientes en la nav.
- Resultado verificado: `selftest OK (6 toolboxes)` sobre el exe real.

**Licencias**

- Añadidos Guillermo, Sabrina, Víctor e Isidro a `emitir_licencias.bat`.

**Docs**

- Nueva guía `docs/guia_gee_project.md`: 5 pasos para que cualquier usuario cree su proyecto GEE desde cero (registro noncommercial → Cloud project → API → vincular → autenticar en M2D), con FAQ y resumen rápido.
  
  
  
  ## Resumen del estado

**🔵 Active (12)** — lo que está abierto para hacer:

- **StoreCox v1** (5): tests de BART, COD (con DMC/Adrian), licenciamiento con Kike, checklist de ofertas, descargar albedos
- **M2D** (4): modelo de nieve `pvlib.snow`, guard de custodia de clave, GTK3 Runtime, fuentes Red Hat Display
- **DEWA/Connectivia** (4): muestra anonimizada para PoC, preguntas de reunión, decisión sobre header, Fase 0 del pipeline

**🟡 Waiting On (2)** — bloqueado por terceros:

- **Barbados 15 MW** → esperando TMY Solargis + albedo de Isidro Torres / Sara Suárez (desde 06-16)
- **Connectivia Labs** → esperando respuesta al correo (enviado ~06-17)

**⚪ Someday (6)** — futuro M2D V2.x, refinamientos opcionales de suciedad, y el "Copiloto Diario" (el SPEC que diseñaste el 06-10).

**✅ Done** — 10 hitos cerrados en el periodo (StoreCox V1, las toolboxes de M2D, el fix del exe del 23, etc.).